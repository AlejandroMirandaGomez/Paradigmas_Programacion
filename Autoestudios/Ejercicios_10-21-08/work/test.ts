// import { $$ } from "./tools.ts"

// $$("__".repeat(10))

// type Fun<T> = (x: T) => (y: T) => T

// type FunNum = Fun<number>

// const foo: FunNum = (x) => (y) => x * y

// const goo: (y: number) => number = foo(666)

// $$("Test: ", goo(10))

// $$("__".repeat(10))
